package aginsun.kingdoms.server.handlers;

import net.minecraftforge.common.config.Configuration;

//TODO: Make the config!!!
public final class ConfigurationHandler
{
    public static final ConfigurationHandler INSTANCE = new ConfigurationHandler();

    public void register()
    {
        //Configuration config = new Configuration();
        //config.load();
        //config.addCustomCategoryComment("construction", "Construction settings");
        //config.get("construction", "fastConstruction", false, "That parametr has been change speed construction");
        //config.get("construction", "speed", 200, "Value for speed construction");
        //config.get("construction", "oldStructures", true, "While no effect");

        //config.addCustomCategoryComment("prices", "Price settings");
        //config.save();
    }
}